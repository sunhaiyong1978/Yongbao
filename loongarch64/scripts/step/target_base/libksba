source env/target_base/config
source env/distro.info
source env/function.sh
export STEP_BUILDNAME=target_base
export STEP_PACKAGENAME=libksba
export PACKAGE_VERSION=1.7.0
export RESOURCEDIR=${NEW_TARGET_SYSDIR}/files/target_base/libksba/1.7.0/
rm -rf ${BUILD_DIRECTORY}/libksba-1.7.0
tar xvf ${DOWNLOADDIR}/libksba-1.7.0.tar.bz2 -C ${BUILD_DIRECTORY} 
pushd ${BUILD_DIRECTORY}/libksba-1.7.0
	
	for config_i in $(find -name "config.sub")
	do
		chmod 0644 $(dirname ${config_i})/config.{sub,guess}
		cp -a -f $(automake --print-libdir)/config.{sub,guess} $(dirname ${config_i})/
	done
	if [ ! -f configure ]; then
		autoreconf -ifv
	fi
	 ./configure ${COMMON_CONFIG}  --enable-malloc0returnsnull
	eval ${MAKE_AND_INSTALL}
popd 
rm -rf ${BUILD_DIRECTORY}/libksba-1.7.0
