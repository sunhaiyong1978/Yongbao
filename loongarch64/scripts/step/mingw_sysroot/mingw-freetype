source env/mingw_sysroot/config
source env/distro.info
source env/function.sh
source env/mingw_sysroot/custom
export STEP_BUILDNAME=mingw_sysroot
export STEP_PACKAGENAME=mingw-freetype
export PACKAGE_VERSION=2.13.2
export RESOURCEDIR=${NEW_TARGET_SYSDIR}/files/mingw_sysroot/mingw-freetype/2.13.2/
rm -rf ${BUILD_DIRECTORY}/freetype-2.13.2
tar xvf ${DOWNLOADDIR}/freetype-2.13.2.tar.xz -C ${BUILD_DIRECTORY} 
pushd ${BUILD_DIRECTORY}/freetype-2.13.2
    sed -ri "s:.*(AUX_MODULES.*valid):\1:" modules.cfg
    sed -r "s:.*(#.*SUBPIXEL_RENDERING) .*:\1:" \
           -i include/freetype/config/ftoption.h
    ./configure ${COMMON_CONFIG} --enable-static \
                --enable-freetype-config
    eval ${MAKE_AND_INSTALL}
popd 
rm -rf ${BUILD_DIRECTORY}/freetype-2.13.2
