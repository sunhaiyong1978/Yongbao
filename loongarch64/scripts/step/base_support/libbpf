source env/base_support/config
source env/distro.info
source env/function.sh
export STEP_BUILDNAME=base_support
export STEP_PACKAGENAME=libbpf
export PACKAGE_VERSION=1.7.0
export RESOURCEDIR=${NEW_TARGET_SYSDIR}/files/base_support/libbpf/1.7.0/
rm -rf ${BUILD_DIRECTORY}/libbpf-1.7.0
tar xvf ${DOWNLOADDIR}/libbpf-1.7.0.tar.gz -C ${BUILD_DIRECTORY} 
pushd ${BUILD_DIRECTORY}/libbpf-1.7.0
	
	make -j${JOBS} CC="${CROSS_TARGET}-gcc" -C src
	make CC="${CROSS_TARGET}-gcc" -C src DESTDIR=${SYSROOT_DIR} install
popd 
rm -rf ${BUILD_DIRECTORY}/libbpf-1.7.0
