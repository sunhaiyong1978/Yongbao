source env/cross-toolchains/config
source env/distro.info
source env/function.sh
source env/cross-toolchains/custom
export STEP_BUILDNAME=cross-toolchains
export STEP_PACKAGENAME=forcheck-PyYAML
export PACKAGE_VERSION=6.0.3
export RESOURCEDIR=${NEW_TARGET_SYSDIR}/files/cross-toolchains/forcheck-PyYAML/6.0.3/
rm -rf ${BUILD_DIRECTORY}/pyyaml-6.0.3
tar xvf ${DOWNLOADDIR}/pyyaml-6.0.3.tar.gz -C ${BUILD_DIRECTORY} 
pushd ${BUILD_DIRECTORY}/pyyaml-6.0.3
	
	${PREFIX}/${LINUX_HOST}/bin/pip3 wheel -w dist --no-build-isolation --no-deps ${PWD}
        ${PREFIX}/${LINUX_HOST}/bin/pip3 install --no-index --find-links dist --no-cache-dir --no-deps --force-reinstall --no-user PyYAML
popd 
rm -rf ${BUILD_DIRECTORY}/pyyaml-6.0.3
