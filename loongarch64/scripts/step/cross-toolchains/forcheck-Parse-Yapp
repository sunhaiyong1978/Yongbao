source env/cross-toolchains/config
source env/distro.info
source env/function.sh
source env/cross-toolchains/custom
export STEP_BUILDNAME=cross-toolchains
export STEP_PACKAGENAME=forcheck-Parse-Yapp
export PACKAGE_VERSION=1.21
export RESOURCEDIR=${NEW_TARGET_SYSDIR}/files/cross-toolchains/forcheck-Parse-Yapp/1.21/
rm -rf ${BUILD_DIRECTORY}/Parse-Yapp-1.21
tar xvf ${DOWNLOADDIR}/Parse-Yapp-1.21.tar.gz -C ${BUILD_DIRECTORY} 
pushd ${BUILD_DIRECTORY}/Parse-Yapp-1.21
	
	export PATH=${PREFIX}/${LINUX_HOST}/bin:${PATH}
	CC=${LINUX_HOST}-gcc \
	${PREFIX}/${LINUX_HOST}/bin/perl Makefile.PL
	eval ${MAKE_AND_INSTALL}
popd 
rm -rf ${BUILD_DIRECTORY}/Parse-Yapp-1.21
