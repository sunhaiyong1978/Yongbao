source env/cross-toolchains/config
source env/distro.info
source env/function.sh
source env/cross-toolchains/custom
export STEP_BUILDNAME=cross-toolchains
export STEP_PACKAGENAME=forcheck-setuptools
export PACKAGE_VERSION=83.0.0
export RESOURCEDIR=${NEW_TARGET_SYSDIR}/files/cross-toolchains/forcheck-setuptools/83.0.0/
rm -rf ${BUILD_DIRECTORY}/setuptools-83.0.0
tar xvf ${DOWNLOADDIR}/setuptools-83.0.0.tar.gz -C ${BUILD_DIRECTORY} 
pushd ${BUILD_DIRECTORY}/setuptools-83.0.0
	
	${PREFIX}/${LINUX_HOST}/bin/pip3 wheel -w dist --no-build-isolation --no-deps ${PWD}
        ${PREFIX}/${LINUX_HOST}/bin/pip3 install --no-index --find-links dist --no-cache-dir --no-deps --force-reinstall --no-user setuptools
popd 
rm -rf ${BUILD_DIRECTORY}/setuptools-83.0.0
